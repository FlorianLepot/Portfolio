{% extends "FLBlogBundle::admin_layout.html.twig" %}

{% block menuBlog %}class="active"{% endblock %}
{% block menuBlogPosts %}class="active"{% endblock %}

{% block body %}
<section class="panel">
    <header class="panel-heading">
        Liste des matières
        <span class="tools pull-right">
            <a href="javascript:;" class="fa fa-chevron-down"></a>
         </span>
    </header>
    <div class="panel-body">
        <table class="table  table-hover general-table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Slug</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('admin_subjects_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td><a href="{{ path('admin_subjects_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                    <td>{{ entity.slug }}</td>
                    <td>
                        <a href="{{ path('admin_subjects_edit', { 'id': entity.id }) }}"  class="btn btn-xs btn-warning"><i class="fa fa-pencil-square-o"></i> Modifier</a>
                        {#<a href="{{ path('admin_posts_delete_get', { 'id': entity.id }) }}"  class="btn btn-xs btn-danger"><i class="fa fa-trash-o"></i> Supprimer</a>#}
                    </td>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>


        <a href="{{ path('admin_subjects_new') }}" class="btn btn-success"><i class="fa fa-plus"></i>
            Créer une matière
        </a>

    </div>
</section>
{% endblock %}
