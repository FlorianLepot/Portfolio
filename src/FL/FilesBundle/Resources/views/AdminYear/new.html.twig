{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Year creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_years') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
